<%= form_with(model: @stay, local: true) do |f| %> <% if @stay.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(@stay.errors.count, "error") %> prohibited this product from being saved:</h2>
        <ul>
            <% @stay.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
        </ul>
    </div>
<% end %>
<div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
</div>
<div class="field">
    <%= f.label :price_per_night %>
    <%= f.text_field :price_per_night %>
</div>
<div class="actions">
    <%= f.submit %>
</div>
<% end %>